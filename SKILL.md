---
name: prompt-enhancer
description: 把用户的模糊需求优化/增强为结构化、可审查、可执行的 prompt，默认只产出文本供用户复制/编辑，不执行任务。触发语境：用户说「优化下这个需求」「帮我增强/润色 prompt」「把这条需求写清楚/说清楚」。排除：优化代码、性能、SQL、流程等与 prompt 本身无关的请求。
---

# Prompt Enhancer（需求增强）

把一条模糊的原始需求，改写成结构化、可直接执行或可移植到其他工具/模型的「增强版 prompt」。设计原则：产物必须能被用户审查、复制或下次使用——因此**默认只产出文本，不执行**。

## 输入边界（默认锁死）

- 被增强对象 = 用户当前消息中描述的需求；可引用本会话已有上下文，但**不扩散到整段历史**。
- **默认不读工作区文件、不调用工具、不写任何文件**，保持"即时文本变换"。
- 需求涉及文件/数据内容时，不主动去读文件：在追问轮请用户贴出关键内容或指明范围。

## 工作流

1. **缺口判定**：若缺少下列之一且会实质影响任务定义——目标受众、交付物形态、关键约束、验收标准——则**一次问完**（≤3 个问题，每个附推荐默认值，最多一轮）；已够具体则**零追问**。
2. **产出「增强版 prompt」**：按下方固定结构输出；中文、一屏可读（约 200–300 字）、放在代码块中。
3. **默认模式 = 只产出**：输出后停止本轮，不执行原任务、不调用工具、不写文件；等待用户复制/编辑，或回复"直接做"。
4. **便利路径（非卖点）**：仅当用户明确说"直接做 / 按此执行"时，才以该增强文本为任务定义去执行。注意同一模型"自产自销"时此路径边际价值最低，**不要主动引导**用户走此路径。

## 「增强版 prompt」固定结构

代码块内按顺序给出以下字段：

- **角色**：谁来做（如"资深数据分析师"）
- **目标**：一句话说清要达成什么
- **输入与可用上下文**：需要什么输入/数据/前提，缺失项标为待提供
- **执行要点**：关键步骤或方法约束
- **输出格式与规范**：交付物形态、格式、篇幅
- **验收标准**：可验证的"做得好"标准
- **明确不做什么**：边界与排除项

示例（关键信息补齐后）：「帮我写个周报」→

```text
角色：项目组周报撰写助手
目标：基于本周工作要点产出一份可直接提交的周报
输入与可用上下文：本周完成事项、进展与数据（由用户补充）；不读文件
执行要点：按 本周进展 → 数据与结果 → 问题与风险 → 下周计划 组织，结果量化
输出格式与规范：中文 Markdown，400 字内，分 4 小节
验收标准：每节含具体事实、无空话，可直接粘贴提交
明确不做什么：不编造数据，不扩展为其他周报模板
```

## Scope 守则

- 优化 ≠ 执行：除非用户说"直接做"，否则不执行原任务。
- 不扩大原任务范围，不替用户做无关工作。

## 方法论出处

字段结构来自通用 prompt engineering 最佳实践与社区对 WorkBuddy「增强提示词」输出结构的实测描述；WorkBuddy 官方未公开内部公式（截至 2026-09 官方文档无相关说明），本 skill 不声称官方出处。
